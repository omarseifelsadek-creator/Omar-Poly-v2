# ══════════════════════════════════════════════
# Polymarket OBI — Dependencies
# ══════════════════════════════════════════════
# Install with: pip install -r requirements.txt
# ══════════════════════════════════════════════

# Real-time WebSocket connection to Polymarket
websockets>=12.0

# Async HTTP client (market discovery + REST fallback)
httpx>=0.27.0

# Numerical computation for analytics
numpy>=1.26.0

# Beautiful terminal UI with live updates
rich>=13.7.0

# Async SQLite for non-blocking database writes
aiosqlite>=0.20.0

# Data validation and typed models
pydantic>=2.6.0

# Environment variable management
python-dotenv>=1.0.0
